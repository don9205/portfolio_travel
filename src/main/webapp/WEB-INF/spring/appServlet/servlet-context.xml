<?xml version="1.0" encoding="UTF-8"?>
<beans:beans
	xmlns="http://www.springframework.org/schema/mvc"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:beans="http://www.springframework.org/schema/beans"
	xmlns:context="http://www.springframework.org/schema/context"
	xsi:schemaLocation="http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc.xsd
		http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd">

	<!-- DispatcherServlet Context: defines this servlet's request-processing 
		infrastructure -->

	<!-- Enables the Spring MVC @Controller programming model -->
	<annotation-driven />

	<!-- Handles HTTP GET requests for /resources/** by efficiently serving 
		up static resources in the ${webappRoot}/resources directory -->
	<resources mapping="/resources/**" location="/resources/" />

	<!-- Resolves views selected for rendering by @Controllers to .jsp resources 
		in the /WEB-INF/views directory -->
	<beans:bean
		class="org.springframework.web.servlet.view.InternalResourceViewResolver">
		<beans:property name="prefix" value="/WEB-INF/views/" />
		<beans:property name="suffix" value=".jsp" />
	</beans:bean>

	<!-- 2019.02.25 이미지 파일 업로드를 위한 multipartResolver 등록 -->
	<beans:bean id="multipartResolver"
		class="org.springframework.web.multipart.commons.CommonsMultipartResolver">
		<beans:property name="maxUploadSize" value="10485760"></beans:property>
		<beans:property name="defaultEncoding" value="UTF-8" />
	</beans:bean>

	<!-- 2019.02.25 실제 이미지가 저장될 경로 uploadPath 추가 -->
	<beans:bean id="uploadPath" class="java.lang.String">
		<beans:constructor-arg
			value="C:\LEE\portfolio_travel\src\main\webapp\resources\file_upload">
		</beans:constructor-arg>
	</beans:bean>


	<context:component-scan
		base-package="kr.lee.travel" />


	<beans:bean id="loginInterceptor"
		class="kr.lee.travel.interceptor.LoginInterceptor"></beans:bean>
	<beans:bean id="AuthInterceptor"
		class="kr.lee.travel.interceptor.AuthInterceptor"></beans:bean>
	<beans:bean id="UnauthInterceptor"
		class="kr.lee.travel.interceptor.UnauthInterceptor"></beans:bean>

	<interceptors>
		<interceptor>
			<mapping path="/" />
			<beans:ref bean="loginInterceptor" />
		</interceptor>
		<interceptor>
			<mapping path="/bbs/travel_board_register" />
<!-- 			<mapping path="/bbs/delete" />
			<mapping path="/bbs/update" /> -->
			<beans:ref bean="AuthInterceptor" />
		</interceptor>
		<interceptor>
			<mapping path="/" />
			<mapping path="/regiser" />
			<beans:ref bean="UnauthInterceptor" />
		</interceptor>
	</interceptors>


</beans:beans>
